apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nuts.fullname" . }}-test-connection"
  labels:
    {{- include "nuts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nuts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
